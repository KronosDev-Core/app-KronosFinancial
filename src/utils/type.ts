const tonum = (val: Number): number => val as number;
const tostr = (val: String): string => val as string;
const tobool = (val: Boolean): boolean => val as boolean;


export { tonum, tostr, tobool };
